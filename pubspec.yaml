name: myapp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  google_fonts: ^8.1.0
  google_sign_in: ^7.2.0
  provider: ^6.1.2

  # Paquetes actualizados a su última versión estable
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  go_router: ^17.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^6.0.0 # Actualizado también

flutter:
  uses-material-design: true
